apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'ThumbjiveAndroidKey'
            storeFile file('/home/elliott/thumbjive/keystores/tj-android.jks')
        }
    }
    compileSdkVersion 25
    buildToolsVersion "25.0.2"
    defaultConfig {
        applicationId 'com.thumbjive.hellovoice'
        minSdkVersion 14
        targetSdkVersion 24
        versionCode 2
        versionName "1.1"
        signingConfig signingConfigs.config
    }
    productFlavors {
    }
    buildTypes {
        release {
            signingConfig signingConfigs.config
        }
    }
}

dependencies {
    compile project(':aars')
    compile project(':models')
    compile 'com.android.support:appcompat-v7:25.2.0'
}
